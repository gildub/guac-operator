apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/app: guac
  name: guac-service
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/app: guac
    